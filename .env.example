# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Auth
BETTER_AUTH_SECRET="your-super-secret-key-at-least-32-characters" # Generate a secure random key by running: openssl rand -base64 32
BETTER_AUTH_URL="http://localhost:3000"

# OIDC Provider
OIDC_CLIENT_ID="your-oidc-client-id"
OIDC_CLIENT_SECRET="your-oidc-client-secret"
OIDC_ISSUER="https://idp.company.local"
OIDC_ADMIN_GROUP="quiz_admin"

# Database
DB_DIALECT="postgres" # "sqlite" (default) or "postgres"
DATABASE_URL="postgresql://quiz_app:securepassword@localhost:5432/quiz_app" # "quiz.db" for SQLite (default), or PostgreSQL connection string
